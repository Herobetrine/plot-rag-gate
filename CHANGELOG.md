# Changelog


## 1.6.4 - 2026-07-20

- Removed the unsupported `SessionEnd` registration from the Codex Hook
  manifest while retaining the internal `--session-end` compatibility and
  diagnostic entry point.
- Moved durable queued, running, failed, and pending-review extraction-barrier
  persistence into the existing `Stop` boundary. The silent persistence pass
  preserves the single Stop response and keeps accepted/no-delta cleanup,
  fail-closed markers, locking, and redaction semantics unchanged.
- Tightened the release contract and regression suite to require exactly the
  current Codex lifecycle events: `SessionStart`, `UserPromptSubmit`, and
  `Stop`.

## 1.6.3 - 2026-07-20

- Removed all repository-tracked project-specific seeds, fixtures, paths, names, source manifests, hashes, and acceptance reports.
- Replaced embedded story examples with deterministic synthetic fixtures and generic identifiers.
- Kept the public Item, Advantage, PowerSpec, source-manifest, proposal/grant/CAS, replay, retrieval, Hook, CLI, and MCP contracts unchanged.

## 1.6.2 - 2026-07-20

- Added a standalone, definition-only PowerSpec import lifecycle. `power-spec validate|preview|propose` accepts a complete `plot-rag-power/v1` aggregate from inline JSON, a UTF-8 file, or stdin, compiles deterministic stable entities and `power_spec` events, and freezes an immutable `power_spec_change` proposal without routing through story initialization.
- Added matching MCP tools: `validate_power_spec_change` and `preview_power_spec_change` are explicitly read-only, while `propose_power_spec_change` only creates the proposal and never issues or consumes a grant. Acceptance continues through `accept_power_spec` host approval, canon-revision CAS, accepted commit, and deterministic replay.
- Kept standalone imports definition-only: actor progression, runtime, ownership, resources, statuses, qualifications, and observations remain story deltas rather than being silently folded into a world-level PowerSpec change. Preview requires an existing accepted continuity state and performs no project creation, schema migration, entity registration, proposal write, or accepted-canon mutation.
- Extended the portable release gate to freeze the PowerSpec compiler schema, collection mapping, hashes and lifecycle constants; the CLI and MCP catalogs; MCP required fields and `readOnlyHint` values; service/runtime integration; and the import, no-state/no-write, atomic-proposal, and canon-CAS regression surfaces.

## 1.6.1 - 2026-07-20

- Added a post-initialization accepted source-manifest lifecycle for source enrollment, correction, metadata changes, and deactivation. `source-manifest status|preview|propose` and three matching MCP tools now feed the existing proposal → host grant → canon CAS → replay path; acceptance requires the dedicated `accept_source_manifest` operation, while the original completion receipt remains immutable.
- Hardened source-manifest proposals with a normalized `frozen_plan` and deterministic re-preview at grant and accept time. The runtime now re-derives the authoritative rollback base, operations, source and entry IDs, semantic actions, counts, hashes, and target-file verification; direct generic-proposal forgery can no longer substitute an arbitrary rollback manifest. Plan revisions and counts now enforce exact JSON integers.
- Made source-manifest status and preview genuinely zero-write by querying a private database snapshot. A config-only project reports that the continuity database is absent without creating `state.sqlite3`, WAL, SHM, or schema side effects; propose remains a non-read-only proposal write and MCP still has no grant issuer.
- Made long-form indexing, initialization verification, Doctor, and replay consume the unique accepted current source version, honor `indexable=false`, accept an approved successor to an immutable initialization receipt, reject replay row tampering, and restore the authoritative pre-migration snapshot on retraction.

## 1.6.0 - 2026-07-18

- Added `plot-rag-advantage/v1`, an independently hashed projection for special items and protagonist advantages. Definitions, anchors, modules, runtime slots and branch state, reward/cost/conversion ledger entries, five-plane knowledge, in-world contracts, narrative contracts, progression, exposure, provenance, reveal stage, and `canon|planned|rumor|misread` status no longer have to be compressed into ordinary item attributes.
- Upgraded continuity storage additively from schema v6 to v7. The established continuity and item projection hashes remain frozen; Advantage uses a third `advantage_projection_hash` with deterministic accepted-event replay, branch isolation, correction, retraction, supersession, migration backup, and fail-closed future-schema handling.
- Added a 16-profile registry covering inheritance, resource transformation, growth relics, pocket domains, companion mentors, system panels, tasks, markets, appraisal/copy, simulation, foreknowledge, time causality, contracts, bloodlines, social currency, and sign-in/lottery mechanisms.
- Added Advantage-aware plot hooks and mandatory context. Relevant plot turns bind the same accepted snapshot to Advantage, Item, Power, relationship, location, story-time, knowledge-plane, and EventExperienceContract context before generation.
- Added strict `Stop` extraction for all thirteen Advantage event families through the mixed `plot-rag-delta/v4` envelope. Mixed output is split in original order into frozen legacy v3 deltas, Item v4 neutral candidates, and Advantage v4 neutral candidates. Remote models emit only closed candidates with mentions, explicit changes, story coordinates, knowledge planes, confidence, and contiguous evidence; local code resolves references, creates valid creator IDs from `artifact_id + reference_type + normalized mention`, binds lifecycle identity, validates runtime math, and produces immutable proposals. Batch adaptation preserves independent valid candidates while locating failures by `candidate_index + adapter_stage`.
- Bound every lifecycle-managed Advantage leaf event to a local four-field EventExperience tuple: `experience_contract_id + experience_contract_hash + event_seed_id + event_seed_revision`. Single-contract groups bind directly; multi-contract groups map deterministic story-coordinate groups to manifest dependency order. Save reports `ADVANTAGE_EXPERIENCE_CONTRACT_REQUIRED` for missing binding; foreign IDs, wrapper/leaf conflicts, and manifest drift report `ADVANTAGE_EXPERIENCE_CONTRACT_MISMATCH`. Grant and accept revalidate the locked identity transactionally, roll back without consuming the grant on failure, and preserve compatibility for legacy proposals, initialization sidecars, direct reducer fixtures, and proposals without lifecycle identity.
- Added eight `advantage` CLI query families, including `advantage anchors` and its `anchor` alias, plus `special-item context|inventory`, the direct `special-item-context` alias, and nine read-only MCP tools for definition, anchors, runtime, modules, ledger, knowledge, progression, exposure, and combined special-item context. Generation visibility always locks Anchor results to active canon; inactive or non-canon rows require explicit inspection/raw audit mode.
- Added the disposable `.plot-rag/金手指/` Markdown projection with deterministic index, definition, module, and runtime cards, plus locked publication, staging recovery, and rollback cleanup.
- Added `V1_6_MIGRATION.md`. State migration receipts bind the database SHA-256, item projection hash, Advantage projection hash, schema-v6 backup, and deterministic item/Advantage readable-projection cleanup instructions.
- Extended the portable release gate to freeze the Advantage schema, config defaults, 16-profile registry, sidecar path/version, CLI/MCP catalog and read-only annotations, migration receipt hashes, rollback cleanup contract, and isolated acceptance assets.
- Final performance evidence: `.plot-rag-benchmark/v16-advantage/20260718T134023Z-offline-final/result.json` passes at `93.9786 / 94.4844 ms` optimized-cold p50/p95 over five measurements and one warmup; `.plot-rag-benchmark/v16-advantage/20260718T134023Z-live-final/result.json` uses SiliconFlow over 20 HTTP requests and passes at `585.5727 / 585.5727 ms`. Both cache gates pass and every selected, critical-fact, stable-ID, mandatory-section, and expected-selection mismatch count is zero.
- Final local regression on the frozen implementation passed with `1126 passed, 10 skipped, 1383 subtests` under pytest and `1136 tests, OK, skipped=10` under unittest with `ResourceWarning` promoted to errors. The staged validate/secret-history/roundtrip/smoke gates, official plugin/skill validators, local `main` fast-forward, personal marketplace reinstall, and source/cache/install byte-equivalence verification also pass.
- Fixed a Windows release-test race in deferred projection failure cleanup: a synchronous drain now waits for the already-running cleanup worker to close its SQLite handle before returning, while preserving the original cancellation error and bounded background retry behavior.

## 1.5.0 - 2026-07-18

- Added a revision- and projection-bound Prepare v2 path with a single accepted-state read snapshot, exact-state short-circuiting, provider-aware exact query Embedding, bounded remote concurrency, per-need failure isolation, bounded parallel Rerank, SingleFlight, revision-aware exact caches, HTTP keep-alive, and per-stage performance telemetry. Batch Embedding is used only for provider/model combinations proven batch-independent; SiliconFlow `api.siliconflow.cn + BAAI/bge-m3` is forced to singleton-exact with concurrency capped by `min(remote_total_concurrency, 4)` and stable input-order assembly.
- Added a process-wide 4096-entry exact-result Rerank LRU keyed by provider identity, model, exact query, ordered documents, `top_n`, and normalization version. Explicit provider identities may share results across wrappers; implicit identities remain bound to the same retained provider object. Failed, interrupted, empty, malformed, duplicate, out-of-range, NaN, or infinite results never enter the healthy cache, and failed flights are cleared for retry.
- Preserved legacy provider order after Rerank; deterministic score/priority/path/ordinal tie-breaking remains limited to candidate and lexical ordering rather than re-sorting provider-ranked results.
- Added durable extraction jobs that bind the assistant artifact, prompt/context digests, canon and projection revisions, intent and event-experience identities, provider/model settings, schema/template hashes, and generation parameters. The default remains synchronous extraction with asynchronous shadowing; strict asynchronous mode preserves the next-plot-turn barrier and still produces proposals rather than accepted canon. `SessionEnd` now persists queued, running, failed, and pending-review barriers to `.plot-rag/session-close-pending.json`, while accepted and no-delta outcomes clear their entries.
- Added `EventSeed`, `EventExperienceArc`, immutable `EventExperienceContract`, one-question structural-ambiguity handling, control-plane CAS, receipt/proposal/grant/accept identity binding, and post-generation `ExperienceReview`. Every designed narrative event now records the intended reader emotion, pressure curve, release pattern, residue, and prohibited misread before expansion. Accepted-outline derivation binds the source commit, artifact ID/version/revision, and content hash, and blocks before receipt creation or remote calls when that identity drifts.
- Added zero-backfill grandfathering for pre-v1.5 accepted artifacts. Existing prose, outlines, canon, commits, and replay hashes remain unchanged; legacy text may receive only a `grandfathered_observed_only` review rather than a fabricated intended contract.
- Isolated ExperienceReview revisioning and failures from lifecycle identity. Recording or superseding a review does not advance the seed/contract binding revision or stale an existing proposal, grant, or accept; automatic-review failures write only redacted diagnostics and never change proposal, extraction-job, or canon state.
- Added a pre-proposal story-artifact boundary for hidden control terms. With story visibility disabled, leaked experience sentinels, schema/type names, or identity fields fail with `STORY_ARTIFACT_CONTROL_TERM_LEAKAGE` before either a proposal or extraction job exists.
- Upgraded continuity storage additively from schema v5 to v6 with replay-derived item definitions, function definitions and bindings, instances, stacks, ownership-versus-custody state, runtime and per-function state, use history, observations, and an independent item projection hash. Migration preserves the established continuity projection hash and never infers missing functions from names or legacy attributes.
- Added `plot-rag-delta/v4` for the new item event family while retaining `plot-rag-delta/v3` and legacy `inventory` behavior. Item use is validated against accepted definition, custody, ownership, unlock, charge, durability, cooldown, qualification, location, binding, and knowledge-plane evidence before acceptance.
- Added canonical JSON Schema entry points for event experience, durable extraction, the mixed delta v4 envelope, and the item initialization sidecar; the v4 remote boundary forbids stable IDs and derived before/after/current/remaining state.
- Added performance, extraction-job, event-experience, and item CLI groups plus eighteen matching MCP tools. Read-only annotations are limited to the three performance tools, extraction list/inspect, experience inspect, and all eight item queries.
- Added `V1_5_MIGRATION.md` covering online v5→v6 backup, frozen legacy continuity hashes, independent item projection hashes, accepted-outline binding, grandfather observed-only reviews, legacy-item non-inference, `.plot-rag/items.v1.json`, staged rollout, verification, and rollback. It explicitly records that v1.4.3 rejects schema v6 with `STATE_SCHEMA_TOO_NEW`, so plugin downgrade requires restoring the schema-v5 backup first.
- Added v1.5 defaults to config v3. Prepare v2 shadow leaves the legacy result authoritative; extraction remains `sync`, and async-shadow proposals are non-accepting and non-blocking; item strict=false performs reducer dry-run diagnostics but blocks v4 authority acceptance before grant consumption while legacy v3 inventory remains compatible. Config v1/v2 lifecycle behavior is unchanged.
- Added an isolated v1.5 live-E2E harness and redacted fixtures covering the four Prepare rollout states, strict Grill→experience→Prepare→extraction→review→accept→replay continuity, project-source immutability, credential scanning, and explicit offline-versus-live result semantics.
- Hardened the live Chat smoke into a semantic gate requiring exactly one correctly anchored movement delta with the expected actor, destination, action, field, empty value, finite confidence, verbatim evidence, no story coordinate, and no skipped or extra delta.
- Redacted common credential shapes, key prefixes, and sensitive environment values before worker, SessionEnd, or experience-review diagnostics are persisted.
- Updated plugin, CLI, and MCP runtime base versions to `1.5.0`, and extended the release gate to lock the v1.5 configuration defaults, `SessionEnd` lifecycle contract, continuity schema v6, item projection schema v1, the v4 item event family, new E2E fixtures, and v3/legacy compatibility.

## 1.4.3 - 2026-07-17

- Made strict Stop proposal construction atomic across mention resolution, entity and alias registration, artifact creation, proposal events and issues, idempotency records, and the final turn-status update. Any failure rolls back the complete transaction, after which only the turn failure marker is written in a separate bounded transaction.
- Added conservative typed-delta normalization for provider-emitted `movement.action=set`: explicit current-location evidence becomes `arrive`, explicit same-clause origin and destination evidence becomes `move`, and negated, planned, historical, belief-scoped, ambiguous, or unbound evidence remains fail-closed. Static arrivals no longer inherit an accepted location as a fabricated origin.
- Hardened story-time extraction so strict label-only time facts populate both `time.value` and `effective_at` without inventing a calendar, empty coordinates normalize to absent, complete structured coordinates are preserved, and partial ordinal/calendar/precision coordinates remain invalid.
- Bounded each strict extraction to at most two provider calls, required `finish_reason=stop`, and added one scope-locked repair path for malformed envelopes, coordinates, or omitted explicit location/time facts. Repair output must stay inside the missing evidence units and allowed event families; extra types, changed evidence, remaining omissions, or a second bad response terminate the turn.
- Made legacy authority indexing honor `authority_sources` with exclude-overrides-include semantics, and remove already-indexed files when their effective policy changes to `exclude`.
- Tightened `HIT_CONFIRMED` so the subject anchor and question focus must coexist inside the same bounded evidence window. Related but mismatched passages remain candidates under `AMBIGUOUS`, while valid facts split across up to three consecutive lines can still be confirmed.
- Added fresh-request Grill skip routing for explicit skip phrases paired with a real plot request, while preserving silence for skip-only text, generic continuation, and genuine requests not to generate or advance plot.
- Updated plugin, CLI, and MCP runtime base versions to `1.4.3`.

## 1.4.2 - 2026-07-17

- Serialized continuity v4-to-v5 migration decisions inside the SQLite `BEGIN IMMEDIATE` lock, re-read the schema version after acquiring the cross-instance mutex, made migration backup paths collision-proof, anchored the opened database identity through backup and commit, and changed deterministic backup publication to atomic no-replace hard links; an existing winner is reused only when a freshly generated, integrity-checked backup is byte-identical, so unrelated valid SQLite files and racing destinations are preserved and rejected. A separate private backup inode now remains anchored until post-commit verification; if the public backup is replaced or rewritten in that final window, the untouched old-schema bytes are atomically preserved under a unique recovery path reported by the error.
- Serialized initialization-storage version checks, additive DDL, and column migration inside one immediate transaction; automatic backups now build and verify a same-directory unique private staging SQLite file, copy it to an independently anchored publication inode, publish with atomic no-replace hard-link semantics, reject SQLite `-journal`/`-wal`/`-shm` sidecar destinations, and never overwrite or remove a racing destination owned by another process. Post-commit replacement or in-place corruption of the public backup likewise preserves the private old-schema copy as a unique recovery backup.
- Enforced exact SQLite table ownership before any backup, DDL, write PRAGMA, dry-run migration, Doctor data read, or component mutation across continuity/state, authority, initialization/remote cache, Grill, long-form memory/method/summary, and projection databases. Foreign, versionless, negative-version, future-version, or incompatible shared-component stores now fail closed without byte changes, while known shared siblings remain compatible.
- Closed every SQLite handle when `row_factory`, `foreign_keys`, `busy_timeout`, `query_only`, `BEGIN IMMEDIATE`, schema setup, commit, or WAL setup raises, including `BaseException` paths. Windows fault-injection tests now prove each affected database can be moved with `os.replace` immediately after failure.
- Preserved v1 initialization sessions through the service path: optional remote-review responses are cached before the first schema-v2 save, the save performs the locked upgrade, and a failed save can retry from the same cache without a second model call.
- Split long-form diagnostics into prepare-refresh policy, query-time provider schema, and observed retrieval outcomes so disabled batch indexing no longer misreports successful SiliconFlow Embedding/Rerank queries as lexical-only.
- Tightened the SiliconFlow typed-delta prompt with the exact action enum for every event family, active-scene `current` versus explicit backstory `historical` rules, and a durable-only `world_rule` boundary; live regression now keeps generated-scene movement and inventory current instead of echoing `movement` as an invalid action or classifying a one-off alarm as a world law.
- Bounded `longform status` projection history, omitted full accepted payloads by default, reported payload byte counts and truncation metadata, and added explicit projection-run inspection for maintainers.
- Preserved dead-owner recovery receipts by disabling constructor auto-recovery on the explicit recovery path, while retaining fail-closed live/unverifiable owner handling and idempotent retry behavior; explicit recovery now filters the mutation itself to the requested run ID and can reconstruct the exact receipt from an already-persisted `interrupted:` terminal row when another journal recovered that same run first. It also uses targeted run and success-record lookups, re-reads concurrent state after recovery, handles cached retry results without a new run ID, and tolerates retained output whose successful journal row has expired.
- Bounded projection status error summaries with explicit UTF-8 byte counts and truncation flags while keeping full diagnostics available through targeted run inspection.
- Made each `(projection, commit, normalized input)` execution an atomic single-owner claim: concurrent callers and recoveries now wait for the claimed run, reuse its result, propagate its terminal failure, and never execute a duplicate projector or let a forced refresh return an older cached output. Projection status rows and totals now come from one SQLite snapshot.
- Treated Linux zombie/exit process states as dead projection owners and added exact-owner CAS cleanup across the durable claim-to-projector handoff, so `KeyboardInterrupt`, post-commit cancellation, and retry handoff interruption cannot overwrite another owner or terminal result. If SQLite locking blocks immediate cleanup, a capped-backoff daemon CAS worker continues until the row becomes terminal or ownership changes, preventing other processes from being stranded behind a still-live owner PID.
- Backed up and rejected non-empty continuity stores that lack schema-version metadata, and made late-created v4 databases enter the same locked, single-backup migration path as pre-existing stores.
- Hardened the release gate with an explicit payload allowlist, LF-byte enforcement for portable text including JSONL fixtures, mandatory release-grade `+codex.<UTC>` metadata, detection of an expected semantic tag already pointing at another commit, and extracted-package CLI/MCP smoke tests across the CI OS/Python matrix. Same-base payload changes now require a different cachebuster both for the staged index relative to `HEAD` and for a clean committed `HEAD` relative to its first parent, preventing distinct install trees from sharing one cache key while still allowing a staged fresh token to repair a stale prior commit; Git comparison failures fail closed. MCP smoke now resolves the actual extracted plugin manifest, MCP manifest, command, arguments, and working directory instead of starting a hard-coded server path, and uses the running interpreter only when that already-validated bare Python alias is absent from the smoke host so Windows/WSL checks remain hermetic.
- Built release payloads and source secret scans from staged Git blobs, failed closed when the index is unavailable or differs byte-for-byte from the worktree, and rejected missing tracked payloads, unresolved stages, Git symlinks, filesystem symlinks, junctions, reparse points, executable modes and other non-`100644` entries before hashing or packaging.
- Rejected absolute, parent-traversing, out-of-root, reparse-backed or unstaged manifest, MCP and hook references; marketplace paths still reject lexical traversal while allowing a configured local alias that resolves to the exact expected source directory.
- Extended that path boundary to MCP commands, every path-like MCP argument, and every hook argument; duplicate JSON keys, Windows-invalid/reserved path components, alternate data streams, source/install root overlap, hard-linked installed files, and every extra installed regular file now fail closed. Marketplace install policy/category values and CI job/step structure are validated rather than accepted by key presence or substring, and portable text rejects lone carriage returns as well as CRLF.
- Made MCP and hook validation exact lifecycle contracts: the single MCP server must directly execute the verified Python script with only the declared fields, while `SessionStart`, `UserPromptSubmit`, and `Stop` must each contain the expected matcher, command, timeout, and no async or duplicate entries. CI now requires unfiltered `push`, `pull_request`, and `workflow_dispatch`, exact permissions, runner/matrix/environment/job fields, an ordered checkout/setup/verification step sequence, credential persistence disabled, full history for release gates, and no extra execution step that could replace the checked source. Installed-tree verification also reports `__pycache__`, `.pyc`, and `.pyo` as executable drift. All automatic and skill-documented installed-cache Python entry points put `-B` before the execution target, and the release gate enforces that ordering.
- Added a Windows-safe cachebuster wrapper that invokes the official plugin-creator helper, restores the semantic base, validates a real 14-digit UTC calendar timestamp, rewrites `plugin.json` with LF bytes, stages it, and transactionally restores both worktree and index state on ordinary failures, `KeyboardInterrupt`, and `SystemExit`; smoke subprocess failures and malformed JSON-RPC results now report structured gate issues instead of tracebacks.
- Corrected the hook trigger contract so maintenance phrases such as “给剧情推演增加测试”, “审查剧情推演流程”, and “优化剧情推演正则” remain silent, while natural webnovel commands such as “续一章”, “再来一章”, “把下一章写出来”, “规划本卷后半段”, and “把章纲扩成正文” enter the same Grill/prepare path. Trigger-wording questions are restricted to known creative commands or an explicit plugin/gate subject, so in-story mechanism questions are not misclassified as plugin work.
- Required `config_version` and legacy `version` to be exact JSON integers `1`, `2`, or `3`; JSON booleans and integral floats no longer compare equal to version `1` and silently downgrade strict lifecycle behavior.
- Required integer-valued project configuration, runtime limits, method-pack and craft-catalog schema versions, benchmark versions and evidence spans, plus package-smoke JSON-RPC response IDs to use exact JSON integers; booleans, integral floats, fractional floats, and numeric strings no longer pass through Python's numeric equality, truncation, or coercion rules.
- Made v1/v2 project-config migration reject non-integer source versions before creating backups, migration receipts, or rewritten configuration.
- Enforced the same exact non-negative integer contract on initialization and Grill handoff revisions, continuity artifact/prepared/canon revisions, power-query chapter/scene positions, and story-coordinate ordinals. Nested power prerequisites now pass through the same coordinate validator, while runtime-owned ability `use_count` rejects model injection and is rebuilt exactly from accepted use events during replay.
- Kept read-side continuity coordinates distinct from write-side artifact coordinates: `chapter_no=0` once again represents the boundary before chapter one for planned-state queries, while booleans, floats, and strings remain rejected. Initialization source byte budgets and in-memory/SQLite remote-cache capacity and TTL bounds now likewise require exact positive integers instead of being silently truncated or coerced.
- Executed each published MCP `tools/call` InputSchema recursively at runtime instead of treating it as documentation only: required fields, closed objects, arrays, exact object/string/integer/boolean types, bounds, enums, lengths, item schemas, and uniqueness now fail before dispatch. Falsy non-object argument payloads are no longer replaced with `{}`, and initialization independently revalidates revisions at the service boundary.
- Pinned GitHub Actions checkout and Python setup dependencies to their verified immutable commit SHAs, with the release gate enforcing those exact workflow contracts.
- Fixed explicit CLI/MCP `project_root` errors so a supplied directory without `.plot-rag/config.json` is reported as a missing project configuration rather than asking for the already supplied argument.
- Updated plugin, CLI, and MCP runtime base versions to `1.4.2`.

## 1.4.1 - 2026-07-16

- Decoupled query-time semantic recall from prepare-time batch indexing so `craft.use_embedding` uses existing authority vectors even when `lifecycle.index_embeddings_on_prepare` is disabled, while retaining rerank and lexical degradation paths.
- Enforced `max_context_chars` against the final long-form continuity envelope, with explicit priority reservations for accepted precise state, open loops, power runtime, and authority memory plus exact budget metadata and bounded large `PowerSpec` rendering.
- Added PID-reuse-resistant projection ownership, migration and completion CAS, single-owner retry, explicit ownerless/dead vector recovery through CLI and MCP, and zero-write Doctor reporting for running or unresolved derived projection failures.
- Fixed `list_power_systems` across MCP and CLI so list calls no longer forward or advertise the query-only `system_id` argument, with a JSON-RPC regression covering the real stdio server entry point.
- Fixed v2-to-v3 project-config migration to non-destructively materialize all thirteen typed state categories, the complete `power_system` defaults, and `auto` initialization-schema negotiation while preserving custom extensions, authority aliases, and replacement semantics.
- Fixed hook arbitration so an active initialization session does not consume unrelated plugin maintenance, analysis, audit, repository, test, or release requests as initialization answers.
- Made frozen initialization proposals report an effective read-only inspect action instead of emitting the misleading `action: answer` label.
- Bound active initialization sessions and turn idempotency to stable host session/turn identities, blocked ambiguous multi-store ownership, and made missing turn identity fall back to read-only inspection instead of revision-derived mutation keys.
- Upgraded initialization persistence to schema v2 content-addressed JSON blobs with SHA-256 verification, bounded zlib decompression, legacy inline compatibility, mandatory online backup, explicit dry-run migration, optional orphan cleanup, and controlled compaction.
- Added `scripts/plot_init_storage.py` for read-only storage inspection and explicit backup-first migration of canonical `.plot-rag/init.sqlite3` databases, with legacy `.plot-rag-init/init.sqlite3` discovery retained.
- Upgraded optional remote-review cache identity to v2 so provider, normalized endpoint, model, user/system prompt, schema, source hash, and every effective generation parameter participate in invalidation; v1 rows are deterministic misses.
- Removed the initialization accept-only runtime path: `init apply` now always materializes the approved bundle after grant and CAS validation.
- Added a standard-library `scripts/release_gate.py` with portable source validation, JSON Schema reference checks, version coordination, high-confidence secret scanning, deterministic package round-trips, marketplace resolution, and source/install-cache hash comparison. Release payloads now come strictly from tracked Git files, while non-ignored untracked release files fail validation explicitly.
- Added a dedicated CI release-gates job with full Git history, minimal permissions, credential persistence disabled, bounded job timeouts, plugin validation, history secret scanning, and package verification.
- Added regression coverage for the release gate, untracked payload rejection, marketplace-relative source resolution, exact secret-fixture allowlisting, masked secret findings, and installed-cache drift detection.
- Made continuity replay regenerate both `continuity_snapshot.json` and the legacy-compatible `state_snapshot.json` from the same accepted current + timeless query, excluding stale legacy `current_facts`, preserving retraction/supersession results and relation dimensions, and deriving stable scope-aware fact keys.
- Updated plugin, CLI, and MCP runtime base versions to `1.4.1`; the final Codex build metadata is generated after review, committed and pushed with the release source, then used for marketplace reinstall and byte-identical cache verification.

## 1.4.0 - 2026-07-16

- Added the default-on `plot-rag-intent/v1` Grill gate before new plot-progression and story-initialization requests.
- Added an eight-field Intent Contract covering the problem, deliverable, reader experience, protagonist drive/conflict, scope endpoint, success criteria, hard constraints, and model autonomy.
- Made Grill ask exactly one unresolved intent question per turn, with a recommended answer and rationale, while reserving project facts such as state, relationships, location, inventory, power, and story time for RAG retrieval instead of user interrogation.
- Added a zero-question fast path for sufficiently specific requests and explicit skip behavior that fills traceable workflow defaults before handoff.
- Added explicit cancel, current-question repeat, and read-only inspect semantics; short prompts such as “继续”, “开始吧”, and “下一步” no longer consume the active answer.
- Restricted continuation inheritance to non-expired `COMPLETED` contracts; unfinished, merely `EXECUTING`, cancelled, or failed handoffs are not reused as shared understanding.
- Added project-local `.plot-rag/grill.sqlite3` storage for non-canonical session progress and turn idempotency, isolated from authority sources, continuity canon, initialization bundles, accepted commits, and replay.
- Integrated Grill with hook arbitration: active initialization remains highest priority, active Grill owns clarification turns, completed or skipped contracts hand off to initialization or plot prepare, and Grill-owned `Stop` turns suppress plot extraction.
- Integrated first-run, configless story initialization with Grill, while preserving active initialization-session continuation and same-turn handoff after contract lock.
- Added `grill.enabled=false` compatibility for projects that need the pre-1.4 direct prepare/initialization flow.
- Added Grill protocol, state-machine, configuration, migration, rollback, initialization, and operational documentation.
- Updated plugin, CLI, and MCP runtime versions to `1.4.0`.

## 1.3.0 - 2026-07-16

- Added the genre-neutral `plot-rag-power/v1` ontology with stable systems, progression tracks, rank nodes and edges, ability and resource definitions, counter/bridge rules, native-term bindings, and actor bootstrap state.
- Added 12 declarative adapters for cultivation, magic, skill trees, game progression, martial arts, superpowers, bloodlines, technology, contract summoning, system-assist stories, hybrid systems, and mundane fiction.
- Upgraded continuity storage to schema v5 with typed `power_spec`, `progression`, `resource`, `status_effect`, `power_binding`, `qualification`, and `power_observation` events.
- Split ability ownership, runtime availability, cooldown, and use history; fixed cross-commit loss/use behavior and preserved persistent level, cost, source, and limit fields.
- Added deterministic endpoint-type, progression-edge, prerequisite, story-coordinate, resource-conservation, immediate-cost, binding, qualification, conversion, and arbitrage validation.
- Added isolated `power_spec_change` proposals and the dedicated `accept_power_spec` grant operation, while keeping ordinary story deltas restricted to actor runtime and observations.
- Added v4-to-v5 migration with online SQLite backup, accepted-event replay, explicit legacy projection provenance, retraction/supersession stability, and reverse ability-owner queries.
- Added `InitializationBundle v2`, automatic v1/v2 negotiation, power-specific adaptive questions and sufficiency gates, lossless `ability.*` claim propagation, and zero-rank mundane initialization.
- Added strict `plot-rag-delta/v3` SiliconFlow extraction for typed power events while retaining local ownership of IDs, evidence, validation, grants, CAS, and accepted writes.
- Added mandatory power context for combat, breakthrough, training, equipment, reward, and contract tasks; expanded long-form memory and the webnovel method pack for progression, resource, cooldown, reveal, and cross-system debt.
- Added `power systems/state/path/explain/compare` CLI commands and matching MCP tools, with condition-based comparison rather than a universal combat score.
- Added a versioned 360-case, 12-profile power benchmark plus focused continuity, initialization, runtime, MCP, migration, replay, and mock-SiliconFlow regression tests.
- Hardened two-stage initialization identity: terminal PowerSpec proposals can be refrozen in a new session, an active exact PowerSpec is reused across sagas, and late idempotent retries preserve the original completion receipt after unrelated canon commits.
- Tightened local ingest ability grammar so institutional authority and ordinary ownership prose do not create actors or ability ownership; verified the fix on an anonymous, zero-write real-work shadow pilot.
- Fixed `power compare --conditions-json` so inline JSON, the default `{}`, JSON files, and stdin all use the same parser contract.
- Added `POWER_SYSTEM_MIGRATION.md` and updated plugin, initialization, CLI, MCP, configuration, and maintenance documentation.

## 1.0.0 - 2026-07-16

- Added config v3 strict lifecycle: generation hooks bind receipts to `prepared_canon_revision`, and `Stop` creates an immutable proposal without changing accepted canon.
- Added one-time host approval grants, token-hash storage, CAS acceptance, idempotent retries, rejection, retraction, correction, supersession, causality links, and deterministic replay.
- Made retraction, superseding acceptance, and replay rebuild long-form memory, chapter/arc/volume summaries, and project craft patterns from active accepted authority commits so inactive revisions are no longer recalled.
- Added artifact-stage, branch, chapter, scene, artifact-revision, canon-revision, current, planned, historical, timeless, provisional, and active projections.
- Added stable entities and aliases plus typed movement, inventory transfer, relationship, ability, belief, commitment, debt, and open-loop events with deterministic endpoint validation.
- Added `new / ingest / hybrid` initialization with four target profiles, three interaction profiles, adaptive “genre → world → plot” questioning, MVW, nine world objects, thirteen runtime modules, five resolution levels, and ten pressure tests.
- Added read-only inventory, source fingerprints, claims, conflict/gap analysis, five knowledge planes, checkpoints, session CAS, stale detection, zero-write dry-run, frozen `InitializationBundle v1` proposals, and per-file diffs.
- Added grant-gated initialization apply, typed bootstrap commits, staging materialization, accepted source manifests, independent projections, completion receipts, `COMPLETED`, and `BOOTSTRAP_READY`.
- Added persistent FTS5/BM25 authority retrieval, SHA-256 incremental refresh, atomic continuity needs, mandatory context contracts, and hard exclusion of runtime, Git, and out-of-root linked paths.
- Added working, episodic, and semantic memory; chapter, arc, and volume summaries; independent snapshot/index/summary/memory/vector projection retry and replay; and bounded retention of rebuildable data only.
- Wired episodic memory, semantic world rules, and chapter/arc/volume summaries into task-aware bounded recall with branch, chapter, arc, volume, and scope labels.
- Added a provenance-tracked nine-card webnovel method pack and accepted-only project pattern learning.
- Added a unified CLI for prepare/propose/query, proposal lifecycle, initialization, long-form retrieval, replay, diagnostics, and schema migration; interactive approval requires two exact full proposal-ID confirmations on a real TTY and exposes no `--yes` bypass.
- Added lifecycle, initialization, and long-form MCP tools; restricted `readOnlyHint` to tools proven to make zero persistent writes; added initialization-aware hooks and config v1/v2 compatibility behavior.
- Added a deterministic 500-chapter fixture and a versioned 240-case end-to-end gate benchmark that starts from assistant proposal text and executes entity/alias resolution, exact evidence validation, the production continuity normalizer, semantic quarantine, accepted-delta scoring, and zero-delta scoring.
- Made accepted outlines unconditionally planned-only, including world-rule candidates that request timeless scope, and exposed planned facts only through an explicit planned query.
- Added optional SiliconFlow initialization ambiguity review behind `PLOT_RAG_INIT_REMOTE_ENABLED=true`; deterministic local extraction always runs first, and remote results remain T4/proposal-only with top-level provenance.
- Made post-commit vector projection execute the real Embedding refresh and report success, degraded lexical fallback, or failed projection runs instead of claiming unconditional success.
- Kept SiliconFlow defaults at `BAAI/bge-m3`, `BAAI/bge-reranker-v2-m3`, and `Qwen/Qwen3-30B-A3B-Instruct-2507`; bound the shared key to `api.siliconflow.cn`, required HTTPS off loopback, blocked credential-bearing redirects, and kept secrets restricted to environment variables.

## 0.3.2 - 2026-07-16

- Made `doctor` and `dump` inspect a stable temporary database/WAL snapshot so the project database, WAL, and SHM remain byte-for-byte unchanged.
- Added structured `not_created` diagnostics for projects that have configuration but no state database.
- Classified generic continuation prompts from the latest effective user task in the Codex transcript while preserving the no-transcript compatibility fallback.
- Kept explicit plot commands higher priority than plugin, workflow, and initialization discussion.
- Added regression coverage for missing storage, active WAL state, SessionStart, transcript variants, injected context, and both directions of continuation inheritance.

## 0.3.1 - 2026-07-15

- Excluded quoted trigger phrases and plugin/process questions from automatic plot preparation.
- Preserved direct commands such as `剧情推演` and `请用插件推演下一章`.
- Added classifier and hook-level regression tests proving meta questions do not create state storage.

## 0.3.0 - 2026-07-15

- Added a provenance-tracked catalog of 14 plot-design method cards distilled from the workspace writing guides.
- Added task detection plus SiliconFlow embedding/rerank retrieval for adaptive method selection before generation.
- Added hidden craft-application guidance that converts methods into causal character choices and state changes without exposing checklist narration in drafts.
- Added craft selections and source traces to turn receipts, SQLite commits, and immutable commit artifacts.
- Added `query_plot_craft` MCP and `plot_state.py craft` CLI access, config validation, doctor coverage, and scenario tests.

## 0.2.0 - 2026-07-15

- Added automatic pre-generation retrieval through `UserPromptSubmit`.
- Added evidence-validated post-generation state commits through `Stop`.
- Added event-sourced character, relationship, location, inventory, story-time, and world-state storage.
- Added OpenAI-compatible embedding, rerank, and extraction adapters with environment-only secrets.
- Added current/planned/historical isolation, immutable turn commits, snapshots, MCP tools, CLI access, and end-to-end tests.
- Added a single-key SiliconFlow profile for embedding, rerank, and strict JSON extraction.
